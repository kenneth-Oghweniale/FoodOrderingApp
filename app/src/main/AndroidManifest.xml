<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kenneth.foodorderingapp">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".application.CartRoomDBApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodOrderingApp">
        <activity android:name=".LandingPage" />

        <activity android:name=".MealDetails" />
        <activity android:name=".OrderSummary" />
        <activity android:name=".CartActivity" />
<!--        <activity android:name=".CheckOut" />-->
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/Theme.FoodOrderingApp.NoActionBar" />
        <activity
            android:name=".SignInActivity"
            android:theme="@style/Theme.FoodOrderingApp.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.FoodOrderingApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SuccessScreenActivity"
            android:theme="@style/Theme.FoodOrderingApp.NoActionBar" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>